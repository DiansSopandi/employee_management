export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
  AGENT = 'AGENT',
  SALES = 'SALES',
  MARKETING = 'MARKETING',
}

export enum Action {
  CREATE = 'CREATE',
  READ = 'READ',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
}

export enum Resource {
  USERS = 'USERS',
  EMPLOYEES = 'EMPLOYEES',
  PRODUCTS = 'PRODUCTS',
  ORDERS = 'ORDERS',
  INVOICES = 'INVOICES',
  SETTINGS = 'SETTINGS',
}
